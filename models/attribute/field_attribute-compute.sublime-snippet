<snippet>
    <content><![CDATA[
    compute='_compute_${1:field}' )

@api.depends('${2:depends}')
def _compute_${1:field}(self):
    for record in self:
        ${4:record.${1:field} = ${3:something}}

${0}
]]></content>
    <tabTrigger>compute</tabTrigger>
    <scope>source.python meta.function-call.arguments.python</scope>
    <description>Field attribute: compute</description>
</snippet>
